<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo">
    <ActionBar title="My Very Epic Video Downloader" icon="" />

    <StackLayout padding="20">
        <!--<Label text="Tap the button" class="h1 text-center" />
        <Button text="TAP" tap="{{ onTap }}" class="-primary" />
        <Label text="{{ message }}" class="h2 text-center" textWrap="true" />-->

        <FlexboxLayout justifyContent="space-between" alignItems="center">
            <TextField id="input" hint="Enter URL" fontSize="14" width="70%" height="50" />
            <Button text="Execute" tap="execute" fontSize="14" width="25%" height="50" />
        </FlexboxLayout>

        <Label id="result" text="" fontSize="14" marginTop="20" />
    </StackLayout>
</Page>
